# v1.6.1 Planning: Deck Definition Infrastructure

**Status:** ✅ COMPLETE
**Goal:** Add DeckDefinition interface and convert RWS to use it internally, preparing for v1.7.0 multi-deck support

---

## Overview

v1.6.0 gave us `CardDefinition` - structured card metadata. Now we need `DeckDefinition` to wrap collections of cards and prepare for future multi-deck support.

**Key principle:** This is internal infrastructure only. No user-facing changes. Zero breaking changes.

---

## Goals

1. Create `DeckDefinition` interface
2. Convert RWS card array to use DeckDefinition structure
3. Maintain 100% backward compatibility
4. Set foundation for v1.7.0 deck switching

---

## DeckDefinition Interface

```typescript
/**
 * Represents a complete deck with metadata
 */
export interface DeckDefinition {
  /** Unique identifier for this deck (e.g., "rider-waite-smith") */
  id: string;
  
  /** Display name of the deck (e.g., "Rider-Waite-Smith") */
  name: string;
  
  /** Optional description of the deck */
  description?: string;
  
  /** Array of all cards in this deck */
  cards: CardDefinition[];
  
  /** Total number of cards (convenience, should match cards.length) */
  cardCount: number;
  
  /** Whether this deck supports reversed cards */
  supportsReversals: boolean;
  
  /** Whether this is a built-in deck */
  isBuiltIn: boolean;
  
  /** Optional metadata about the deck */
  metadata?: {
    /** Deck author/creator */
    author?: string;
    /** Publication year */
    year?: number;
    /** Publisher */
    publisher?: string;
    /** Deck tradition (tarot, oracle, lenormand, runes, etc.) */
    tradition?: string;
    /** Any other custom metadata */
    [key: string]: unknown;
  };
}
```

---

## Implementation Plan

### Phase 1: Add DeckDefinition to types

**File:** `src/types/deck.ts`

Add the interface alongside existing `CardDefinition`.

### Phase 2: Convert RWS to DeckDefinition

**Current state (src/core/CardDatabase.ts):**
```typescript
export const RWS_CARDS: CardDefinition[] = [
  { index: 0, name: "The Fool", ... },
  // ... 77 more cards
];
```

**Target state:**
```typescript
export const RWS_DECK: DeckDefinition = {
  id: "rider-waite-smith",
  name: "Rider-Waite-Smith",
  description: "The classic tarot deck created by Arthur Edward Waite and illustrated by Pamela Colman Smith",
  cards: [
    { index: 0, name: "The Fool", ... },
    // ... 77 more cards
  ],
  cardCount: 78,
  supportsReversals: true,
  isBuiltIn: true,
  metadata: {
    author: "Pamela Colman Smith",
    year: 1909,
    publisher: "Rider & Company",
    tradition: "tarot"
  }
};

// Backward compatibility - keep the array export
export const RWS_CARDS = RWS_DECK.cards;
```

### Phase 3: Update utility functions (if needed)

**Ensure these still work:**
- `getCard(index: number): CardDefinition`
- `getCardName(index: number): string`

**Possibly add:**
- `getDeck(): DeckDefinition` - returns current deck (RWS for now)
- `getDeckInfo(): { id: string, name: string, cardCount: number }` - deck metadata

### Phase 4: Documentation

Update planning docs to reflect completion:
- Mark Phase 1 complete in this doc
- Update v1.6.0 planning with pointer to v1.6.1
- Note in CHANGELOG.md

---

## Backward Compatibility

**Critical:** No breaking changes!

✅ `RWS_CARDS` array still exists (now points to `RWS_DECK.cards`)
✅ All existing functions work exactly the same
✅ No settings changes
✅ No template changes
✅ No user-visible changes

**Testing checklist:**
- [ ] All spreads still work
- [ ] Card draws still work
- [ ] Templates still render correctly
- [ ] No TypeScript errors
- [ ] No ESLint errors

---

## What This Enables for v1.7.0

Once DeckDefinition exists, v1.7.0 can add:
- Deck registry/manager
- Multiple deck definitions
- UI for deck selection
- Per-spread deck choice
- User-added custom decks

---

## Success Criteria

v1.6.1 is successful when:

✅ DeckDefinition interface exists in `src/types/deck.ts` - DONE
✅ RWS_DECK constant created with full metadata - DONE
✅ RWS_CARDS still exported for backward compatibility - DONE
✅ All existing functionality works unchanged - DONE (testing complete)
✅ Zero breaking changes - DONE
✅ Zero ESLint errors - DONE
✅ Documentation updated - DONE (CHANGELOG.md updated)

---

## Timeline Estimate

**Total time:** 1-2 hours

- Phase 1 (Interface): 15 minutes
- Phase 2 (RWS conversion): 30 minutes  
- Phase 3 (Utilities): 15 minutes
- Phase 4 (Documentation): 15 minutes
- Testing: 15 minutes

---

## Notes

- This is pure infrastructure - no UI changes
- Sets clean foundation for v1.7.0
- Maintains 100% backward compatibility
- Simple, focused scope
