# v1.8.1 Planning: Spread Import/Export

**Status:** Planning  
**Branch:** `feature/spread-import-export` (to be created)  
**Target Release:** TBD

## Overview

Add import/export functionality for custom spreads, mirroring the deck system from v1.8.0. Enable community sharing of spread definitions with optional template files.

## Goals

- **User Experience:** Easy spread sharing via JSON or ZIP files
- **Community Building:** Enable spread marketplace/repository
- **Template Bundling:** Include custom templates with spread exports
- **Backward Compatibility:** Existing spreads in settings continue to work
- **Familiar UX:** Mirror deck management interface

## User Workflows

### Export Workflow
```
1. Settings → Tarot Practice → Spread Management
2. Click on custom spread in list
3. Click "Export" button
4. Choose format: JSON only or ZIP with template
5. File downloads to computer
6. User can share on GitHub, Discord, forums, etc.
```

### Import Workflow
```
1. User downloads spread file from community
2. Settings → Tarot Practice → Spread Management
3. Click "Import spread" button
4. Select JSON or ZIP file
5. Spread validates and installs
6. Immediately available in spread selector
```

## Technical Approach

### File Structure

**Spread storage location:**
```
.obsidian/plugins/tarot-practice/
├── decks/              # Existing deck storage
│   └── {deck-id}/
│       └── deck.json
└── spreads/            # New spread storage
    └── {spread-id}/
        └── spread.json
```

**ZIP structure for sharing:**
```
celtic-cross.zip
├── spread.json         # Required: Spread definition
└── template.md         # Optional: Custom template for this spread
```

**Note on templates in vault:**
If spread.json references a templatePath, that's a vault path. The bundled template.md in the ZIP is imported to the vault at: `{templateBaseFolder}/Spreads/{spread-id}/template.md`

### Spread JSON Structure

```json
{
  "id": "custom-relationship",
  "name": "Relationship Spread",
  "description": "Explores dynamics between two people",
  "positions": [
    {
      "label": "You",
      "description": "How you show up in this relationship"
    },
    {
      "label": "Them", 
      "description": "How they show up in this relationship"
    },
    {
      "label": "Connection",
      "description": "The energy between you"
    }
  ],
  "shuffleCount": 3,
  "cutDeck": true,
  "templatePath": "Templates/Tarot/Spreads/relationship/template.md",
  "isBuiltIn": false,
  "metadata": {
    "author": "Jane Doe",
    "year": 2025,
    "tradition": "modern",
    "source": "Original creation"
  }
}
```

### Implementation Tasks

**Core Classes:**
- [x] Spread interface exists in `src/core/spreads.ts`
- [x] Create `SpreadValidator` class (validation logic)
  - [x] Validate spread structure
  - [x] Check for duplicate IDs  
  - [x] Validate position array
  - [x] Check metadata completeness
- [x] Create `SpreadLoader` class (import/export logic)
  - [x] `installFromJSON(file)` - Install spread from JSON
  - [x] `installFromZIP(file)` - Install spread + template from ZIP
  - [x] `exportSpread(spread, includeTemplate)` - Export to JSON or ZIP
  - [N/A] `loadAllSpreads()` - Not needed (using settings for now)
  - [N/A] `removeSpread(spreadId)` - Not needed (manual deletion via settings)
  - [N/A] `spreadExists(spreadId)` - Not needed (checked via settings)

**Settings Integration:**
- [x] Add "Import spread" button to Spread Management section
- [x] Add "Export" button to each custom spread
- [N/A] Migration: No users exist yet, no migration needed
- [N/A] Keep `settings.customSpreads` for now (can deprecate in v2.0 if needed)
- [x] Load spreads from settings via SpreadResolver (existing behavior)

**UI Components:**
- [x] Update `TarotPracticeSettingTab.ts` with import/export buttons
- [x] File picker for JSON/ZIP selection
- [x] Export format modal (JSON only vs ZIP with template)
- [x] Validation error display
- [x] Success/failure notices

**Template Handling:**
- [x] When exporting as ZIP, include template file if spread has templatePath
- [x] When importing ZIP, extract template to `{templateBaseFolder}/Spreads/{spread-id}/`
- [x] Update spread.json templatePath to point to extracted location
- [x] Handle case where template doesn't exist (use built-in)

**Migration Strategy:**
- [N/A] No migration needed - zero users currently exist
- Current behavior: Spreads stored in `settings.customSpreads` (works fine)
- Future: Can deprecate settings-based spreads in v2.0 if file-based becomes preferred
- For now: SpreadLoader handles import/export, SpreadResolver continues reading from settings

### Storage Strategy

**Hybrid approach (like decks):**
- Spread definition in `.obsidian/plugins/tarot-practice/spreads/{spread-id}/spread.json`
- Optional template in vault at `{templateBaseFolder}/Spreads/{spread-id}/template.md`

**Benefits:**
- Spread configs separate from vault (plugin-managed)
- Templates in vault (user can edit, sync, use wikilinks)
- Clean uninstall removes both config and template
- Template base folder respects user settings

### Validation Rules

**Required fields:**
- `id` - lowercase, hyphens only, unique
- `name` - display name
- `positions` - array with at least 1 position
- `positions[].label` - required for each position

**Optional but recommended:**
- `description` - what the spread is for
- `positions[].description` - meaning of each position
- `metadata.author` - who created it
- `metadata.tradition` - tarot, oracle, etc.

**Position validation:**
- At least 1 position required
- Maximum 78 positions (full deck)
- Each position must have a label
- Position order matters (index 0, 1, 2...)

### Edge Cases

- [ ] Spread ID collision → Prompt to overwrite or rename
- [ ] ZIP without spread.json → Error with clear message
- [ ] ZIP with invalid spread.json → Validation errors shown
- [ ] Template path in spread.json doesn't exist → Use built-in
- [ ] Corrupted ZIP → Catch errors, show notice
- [ ] Large ZIP files → Handle async, show progress
- [ ] Export spread without template → JSON export only
- [ ] Import spread with duplicate position labels → Warning (allowed)

## Testing Plan

- [ ] Export spread as JSON
- [ ] Import JSON-only spread
- [ ] Export spread with template as ZIP
- [ ] Import ZIP spread with template
- [ ] Import spread with ID collision
- [ ] Import corrupted ZIP
- [ ] Import ZIP missing spread.json
- [ ] Import ZIP with invalid spread.json
- [ ] Verify template extraction to correct location
- [ ] Verify imported spread works in draws
- [ ] Test migration from settings.customSpreads
- [ ] Test removal cleans up both config and template
- [ ] Test on desktop (mobile support lower priority)

## Documentation Updates

- [ ] README.md - Mention spread import/export
- [ ] docs/SETTINGS.md - Update Spread Management section
- [ ] docs/USAGE.md - Add "Sharing Spreads" section
- [ ] CHANGELOG.md - Add v1.8.1 entry
- [ ] Create CREATING-SPREADS.md guide (mirror CREATING-DECKS.md)

## Release Checklist

- [ ] All tests passing
- [ ] Documentation updated
- [ ] CHANGELOG.md updated
- [ ] Version bumped to 1.8.1
- [ ] Build successful
- [ ] Merge to master
- [ ] Tag created (1.8.1)
- [ ] GitHub release
- [ ] Create example spreads for community

## Community Spreads Repository

**Future consideration:** Create `obsidian-tarot-spreads` repository (mirror of `obsidian-tarot-decks`)

**Initial spreads to include:**
- Relationship spread (3 cards)
- Career crossroads (5 cards)
- Shadow work (7 cards)
- Horseshoe spread (7 cards)
- Tree of Life (10 cards)

## API for Future Extensions

The spread system could enable:
- Spread collections/packs
- Spread marketplace
- Versioning and updates (via sourceUrl like decks)
- User ratings and reviews
- Spread categories/tags

## Notes

- Keep it simple initially - just import/export
- Community will drive which spreads are popular
- Template bundling makes spreads instantly usable
- ZIP format enables rich sharing (spread + template + docs)
- JSON format for simple/lightweight spreads

---

**Key insight:** Spreads are lighter weight than decks (no images), so ZIP format is mainly for bundling templates rather than assets. Most spreads might be shared as simple JSON.
